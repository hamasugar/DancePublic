# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Dance' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Dance

  target 'DanceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DanceUITests' do
    inherit! :search_paths
    # Pods for testing
  end
	pod "Alamofire"
	pod "SwiftyJSON"
	pod 'AWSS3'
	pod 'Stripe'
	pod 'Cosmos'

end
